---
import { GoogleAnalytics } from '@astrolib/analytics';
import { ANALYTICS } from "~/utils/config";

---

<!-- Google Analytics -->
{ANALYTICS?.vendors?.googleAnalytics?.id && <GoogleAnalytics id={String(ANALYTICS.vendors.googleAnalytics)} partytown={true} />}